"""Sanity check that the pytest/ruff harness runs on a clean checkout.

Delete once real tests land (M2, issue 7 — test suite for the container).
"""


def test_placeholder() -> None:
    assert True
